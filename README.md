# ai-workflow-assignment
“This project is a simple workflow engine built with Python and FastAPI. It runs a sequence of steps, each updating a shared state. I kept the design clean and easy to follow. A small code-review workflow is included to show how nodes, edges, branching, and loops work in practice.
